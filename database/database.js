class Database{
  constructor() {}

//  writeURL(url) { }

  checkExists(fullUrl) {
   const database = `./database/DB.json`
  if (database.checkExists({ fullUrl }.json)) {
    return true;
  } else {
    return false;
  }
}

 isExists((req, res) => {
    if (fullUrl === true) {
        return res.shortUrl;
    } else {
      createShortUrl();
    }
});

 getUrl(url) {}

function createShortUrl(fullUrl) {

 }

}